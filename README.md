# grayout-page

Public site + update feed for **Grayout** — a macOS menu-bar app that grays out
your screen to reduce color/notification distraction.

- Served via GitHub Pages (custom domain: **grayout.cc**, once registered).
- Hosts the landing page (`index.html`), the Sparkle update feed (`appcast.xml`),
  and release downloads.
- The app's source lives in the **private** `caasols/grayout` repo; only the
  public website + binaries live here.
